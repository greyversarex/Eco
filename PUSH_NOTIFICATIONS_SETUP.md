# 🔔 Push Notifications Setup Guide

## 📋 Обзор

Push уведомления позволяют пользователям получать уведомления о новых сообщениях, заданиях и объявлениях даже когда приложение закрыто.

---

## ⚙️ Генерация VAPID ключей

### Шаг 1: Установите web-push глобально (если еще не установлен)

```bash
npm install -g web-push
```

### Шаг 2: Сгенерируйте VAPID ключи

```bash
npx web-push generate-vapid-keys
```

Вы получите вывод вида:

```
=============== VAPID ключи ===============

Public Key:
BKxWL8jVb9k4LqF...

Private Key:
XYZ123abc...

=======================================
```

---

## 🔐 Настройка переменных окружения

### Для Production сервера (Timeweb)

Добавьте следующие переменные в `.env` на сервере:

```env
# VAPID ключи для Push Notifications
VAPID_PUBLIC_KEY=ваш_публичный_ключ_здесь
VAPID_PRIVATE_KEY=ваш_приватный_ключ_здесь
VAPID_SUBJECT=mailto:admin@ecodoc.tj
```

### Для Frontend (Replit Secrets)

В Replit Secrets добавьте:

```
VITE_VAPID_PUBLIC_KEY=тот_же_публичный_ключ
```

⚠️ **ВАЖНО**: 
- `VAPID_PUBLIC_KEY` (для backend) и `VITE_VAPID_PUBLIC_KEY` (для frontend) должны быть **одинаковыми**
- `VAPID_PRIVATE_KEY` НЕ должен быть доступен на frontend (только на backend)

---

## 📱 Как работают Push Notifications

### 1. Подписка пользователя

Когда пользователь входит в приложение:
1. Приложение запрашивает разрешение на уведомления
2. Если пользователь разрешает → создаётся подписка через Service Worker
3. Подписка отправляется на сервер и сохраняется в БД

### 2. Отправка уведомлений

Когда нужно отправить уведомление:

```typescript
// Пример интеграции в routes.ts при создании сообщения
const sendPushNotification = (app as any).sendPushNotification;

if (sendPushNotification) {
  // Отправить уведомление получателям
  for (const recipientId of message.recipientIds) {
    await sendPushNotification(
      recipientId,
      'department',
      {
        title: 'Новое сообщение',
        body: `От: ${senderDept.name}`,
        url: `/department/inbox`,
      }
    );
  }
}
```

### 3. Получение уведомлений

Service Worker получает push event и показывает уведомление пользователю, даже если приложение закрыто.

---

## 🔧 Интеграция в код

### Добавление уведомлений при создании сообщения

Найдите endpoint создания сообщений (`/api/messages`) в `server/routes.ts`:

```typescript
app.post("/api/messages/broadcast", requireAuth, upload.array('files', 5), async (req: Request, res: Response) => {
  try {
    // ... существующий код создания сообщения ...
    
    // Добавьте отправку push уведомлений
    const sendPushNotification = (app as any).sendPushNotification;
    if (sendPushNotification) {
      const sender = await storage.getDepartmentById(user.id);
      
      for (const recipientId of validatedData.recipientIds) {
        await sendPushNotification(
          recipientId,
          'department',
          {
            title: 'Новое сообщение',
            body: `От: ${sender?.name || 'Неизвестно'}`,
            url: '/department/inbox',
          }
        ).catch(err => console.error('Push notification failed:', err));
      }
    }
    
    res.json(newMessage);
  } catch (error: any) {
    // ...
  }
});
```

### Добавление уведомлений при создании задания

Аналогично для endpoint создания заданий (`/api/assignments`):

```typescript
// После создания assignment
if (sendPushNotification) {
  for (const recipientId of validatedData.recipientIds) {
    await sendPushNotification(
      recipientId,
      'department',
      {
        title: 'Новое задание',
        body: validatedData.topic,
        url: '/department/assignments',
      }
    ).catch(err => console.error('Push notification failed:', err));
  }
}
```

---

## ✅ Проверка работы

### 1. Проверьте настройку VAPID ключей

При запуске сервера вы должны увидеть:
```
✅ Push notifications configured
```

Если нет:
```
⚠️ VAPID keys not configured. Push notifications will not work.
```

### 2. Проверьте подписку в браузере

1. Откройте DevTools → Application → Service Workers
2. Убедитесь что Service Worker активен
3. Откройте Application → Push Messaging
4. Должна быть активная подписка

### 3. Проверьте базу данных

```sql
SELECT * FROM push_subscriptions;
```

Должны быть записи с endpoint, p256dh, auth для подписанных пользователей.

### 4. Протестируйте отправку

Создайте новое сообщение и проверьте:
- Получатель должен получить push уведомление
- При клике на уведомление → открывается приложение

---

## 🐛 Troubleshooting

### Уведомления не приходят

1. **Проверьте VAPID ключи**:
   ```bash
   echo $VAPID_PUBLIC_KEY
   echo $VAPID_PRIVATE_KEY
   echo $VITE_VAPID_PUBLIC_KEY
   ```
   Публичные ключи должны совпадать.

2. **Проверьте разрешения браузера**:
   - Chrome: Settings → Privacy → Site Settings → Notifications
   - Убедитесь что сайт разрешён

3. **Проверьте Service Worker**:
   - DevTools → Application → Service Workers
   - Service Worker должен быть активен

4. **Проверьте логи сервера**:
   ```bash
   # На сервере
   pm2 logs ecodoc --lines 100
   ```

### "Subscription is no longer valid"

Это означает что подписка устарела (например, пользователь очистил кэш браузера). Система автоматически удалит такие подписки при попытке отправки.

---

## 📊 Мониторинг

### Проверка количества активных подписок

```sql
SELECT 
  user_type,
  COUNT(*) as subscription_count,
  COUNT(DISTINCT user_id) as unique_users
FROM push_subscriptions
GROUP BY user_type;
```

### Очистка устаревших подписок

Система автоматически удаляет подписки которые вернули ошибку 410 (Gone).

---

## 🔒 Безопасность

- ✅ VAPID приватный ключ НЕ доступен на frontend
- ✅ Подписки привязаны к конкретному пользователю
- ✅ Пользователь может отписаться только от своих подписок
- ✅ Все API endpoints требуют авторизации

---

## 📝 Дополнительная информация

- [Web Push Protocol](https://developers.google.com/web/fundamentals/push-notifications)
- [web-push npm package](https://www.npmjs.com/package/web-push)
- [Service Worker API](https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API)

---

**Готово! Push уведомления настроены и готовы к использованию** 🎉
