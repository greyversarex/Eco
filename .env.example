# ===================================
# ЭкоТочикистон - Production Environment Configuration
# ===================================

# -------------------------------------
# PostgreSQL Database (Neon)
# -------------------------------------
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Individual database connection parameters (auto-populated from DATABASE_URL)
PGHOST=your-neon-host.neon.tech
PGPORT=5432
PGUSER=your-username
PGPASSWORD=your-password
PGDATABASE=your-database

# -------------------------------------
# Supabase Storage Configuration
# -------------------------------------
# Get these from your Supabase project settings:
# Project URL: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project-ref.supabase.co

# Service Role Key (NOT anon key!) - use this for server-side uploads
# Found in: Project Settings > API > service_role key (keep secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Storage bucket name (create this bucket in Supabase Storage dashboard)
# Default bucket for file attachments
SUPABASE_STORAGE_BUCKET=ecotajikistan-files

# -------------------------------------
# Application Security
# -------------------------------------
# Generate a random secret for session encryption
# You can use: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-here

# -------------------------------------
# Environment
# -------------------------------------
# Set to 'production' on your VDS server
NODE_ENV=production

# -------------------------------------
# Optional: Port Configuration
# -------------------------------------
# The application will run on this port (default: 5000)
PORT=5000

# ===================================
# SETUP INSTRUCTIONS
# ===================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in all the values above with your actual credentials
#
# 3. Create Supabase Storage Bucket:
#    - Go to https://app.supabase.com/project/_/storage/buckets
#    - Click "New bucket"
#    - Name: ecotajikistan-files
#    - Public bucket: NO (keep it private)
#    - Click "Create bucket"
#
# 4. Set up Storage RLS Policies (if needed):
#    - By default, Service Role Key bypasses RLS
#    - For production, consider adding RLS policies for extra security
#
# 5. Never commit .env file to git!
#    It's already in .gitignore
#
# ===================================
