# Database Configuration
# PostgreSQL connection string (REQUIRED)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/ecotajikistan

# Session Secret (REQUIRED)
# Replit auto-generates this in development
# For production on Timeweb: Generate a secure random string:
# openssl rand -base64 64
SESSION_SECRET=

# Node Environment (OPTIONAL)
# Set to 'production' for production deployment
NODE_ENV=development

# API URL for Mobile Apps (REQUIRED for mobile builds)
# This should point to your production server on Timeweb
# Example: https://ecodoc.example.com
# For local development: leave empty or use http://localhost:5000
VITE_API_URL=
