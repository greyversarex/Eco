# 🔄 Инструкция по безопасному обновлению продакшена

## ✅ Процесс обновления (одна команда)

```bash
cd ~/Eco && git reset --hard && git pull && npm install && npm run db:migrate && npm run build && pm2 restart ecotajikistan
```

## 📋 Подробное объяснение каждого шага

### 1. Переход в директорию проекта
```bash
cd ~/Eco
```

### 2. Сброс локальных изменений
```bash
git reset --hard
```
**Что делает:** Удаляет все ваши ручные правки, чтобы не было конфликтов с новой версией.

### 3. Получение новой версии кода
```bash
git pull
```
**Что делает:** Скачивает обновленный код из GitHub.

### 4. Обновление зависимостей
```bash
npm install
```
**Что делает:** Устанавливает новые пакеты, если они были добавлены в новой версии.

### 5. 🔐 Применение миграций БД (БЕЗОПАСНО!)
```bash
npm run db:migrate
```
**Что делает:** 
- ✅ Добавляет новые таблицы/колонки если нужно
- ✅ НЕ удаляет существующие данные
- ✅ НЕ удаляет департаменты
- ✅ НЕ удаляет сообщения и файлы
- ✅ Если изменений нет - ничего не делает

**ВАЖНО:** Это НЕ `npm run db:seed`! Seed только для локальной разработки.

### 6. Пересборка проекта
```bash
npm run build
```
**Что делает:** Компилирует обновленный код для production.

### 7. Перезапуск приложения
```bash
pm2 restart ecotajikistan
```
**Что делает:** Перезапускает сайт с новым кодом.

---

## 🛡️ Гарантии безопасности

### ✅ Что НЕ будет удалено при обновлении:

1. **Департаменты** - все 50 департаментов сохранятся
2. **Сообщения** - все отправленные документы останутся
3. **Файлы** - все вложения сохранятся
4. **Задания (Супоришҳо)** - все поручения останутся
5. **Объявления** - все объявления сохранятся
6. **Пользователи** - админ и все коды доступа останутся

### ✅ Что ДОБАВИТСЯ при обновлении:

1. **Новые функции** в коде
2. **Новые поля в БД** если нужно (например, documentNumber)
3. **Новые индексы** для ускорения работы
4. **Недостающие департаменты** если их меньше 50

---

## 📊 Система департаментов

**Текущее состояние: 49 департаментов** в системе, разделенных на 4 блока:

| Блок | Название | Текущее | Требуется |
|------|----------|---------|-----------|
| Upper | Кумитаи ҳифзи муҳити зист | 8 | 8 ✅ |
| Middle | Раёсатҳо | 12 | 11 ⚠️ |
| Lower | Муссисаҳои тиҷоратӣ... | 16 | 18 ⚠️ |
| District | Ноҳияҳои тобеи марказ | 13 | 13 ✅ |

**Примечание:** Файл пользователя содержит 49 департаментов. Ожидается уточнение списка для достижения требуемых 50 (8/11/18/13).

**Примеры кодов:**
- `ROHBAR001` - Роҳбарият
- `KADR001` - Раёсати кадрҳо
- `DIGITAL001` - Раёсати рақамикунонӣ
- `DUSHANBE001` - Сарраёсати ш. Душанбе

---

## 🔍 Проверка после обновления

```bash
# Проверить логи приложения
pm2 logs ecotajikistan --lines 50

# Проверить статус
pm2 status

# Проверить что сайт доступен
curl http://localhost:5000
```

---

## ⚙️ Доступные команды

| Команда | Для чего | Где использовать |
|---------|----------|------------------|
| `npm run db:migrate` | Применить миграции БД | ✅ Production |
| `npm run db:push` | Push схемы напрямую | ❌ Только локально |
| `npm run db:seed` | Заполнить данными | ❌ Только локально |
| `npm run db:generate` | Сгенерировать миграцию | ❌ Только локально |
| `npm run build` | Собрать проект | ✅ Production |
| `npm start` | Запуск production | ✅ Production |

---

## 🆘 Что делать если что-то пошло не так?

### Откат к предыдущей версии

```bash
# 1. Посмотреть историю коммитов
git log --oneline

# 2. Вернуться к предыдущей версии (заменить <hash> на хэш коммита)
git reset --hard <hash>

# 3. Пересобрать и перезапустить
npm run build && pm2 restart ecotajikistan
```

### Резервная копия БД

**Перед большим обновлением делайте backup:**

```bash
# Создать backup
pg_dump $DATABASE_URL > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить если нужно
psql $DATABASE_URL < backup_YYYYMMDD_HHMMSS.sql
```

---

## 📝 История изменений (Changelog)

### Версия от 07.11.2025

✅ **Добавлено:**
- Поле `documentNumber` (Рақами ҳуҷҷат) для сообщений и заданий
- Обновлен список департаментов (50 шт)
- Улучшен UI формы создания сообщений
- Удалена страница мониторинга

✅ **Исправлено:**
- Терминология: "Нест кардан" → "Бекор кардан"
- Убрана обязательность поля "Мундариҷа"
- Улучшены hover-эффекты сообщений

⚠️ **Требуется миграция БД:** Да (добавляет поле document_number)

---

## 🎯 Контрольный список обновления

- [ ] Сделан backup базы данных
- [ ] Выполнена команда обновления
- [ ] Проверены логи `pm2 logs`
- [ ] Сайт открывается в браузере
- [ ] Можно войти под админом
- [ ] Можно войти под департаментом
- [ ] Можно отправить сообщение
- [ ] Можно создать задание (если есть права)

---

**Всё готово!** Ваш сайт обновлен безопасно и без потери данных. 🎉
